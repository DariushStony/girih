<!-- Generated by docs/scripts/build-docs.mjs — edit docs/scripts/pages/07-error-codes.mjs instead. -->

# Every error code

> All 70 GIRIH diagnostics, extracted from source, with what each one is actually telling you.
>
> **This is the Markdown mirror.** The [interactive version](../07-error-codes.html) has live
> widgets — a brand switch, an alias-chain walker, a pipeline stepper, and a quiz — that
> Markdown cannot render.

← [The code](06-the-code.md)  ·  [Contents](README.md)  ·  [Check yourself](08-quiz.md) →

---

All 71 diagnostics girih can emit — 58 errors and
13 warnings — extracted straight from
`packages/*/src` by a script. The messages below are the real strings, not paraphrases.

> **🔵 Rule — This page is generated**
>
> `docs/scripts/extract-diagnostics.mjs` parses every
> `{ code: 'GIRIH…', severity, message, help }` object literal out of the TypeScript
> sources and writes `docs/data/diagnostics.json`, which this page renders. Re-run it
> after changing any diagnostic; `--check` exits non-zero if the JSON is stale, so it can
> guard a commit.
>
> A hand-written reference would go quietly wrong the first time somebody improved a
> `help` string. This one cannot.

## Anatomy of a diagnostic

girih almost never throws. Problems are values with a stable shape, which is what makes them
greppable, testable, and machine-readable:

**[Terminal]**

```
error GIRIH2030: 'button.primary.background' references '{color.primaryy}', which does not exist.
      │         │      └─ message, with the offending path interpolated
      │         └─ stable code — safe to grep for, safe to assert in a test
      └─ severity: error | warning | info
  help: Did you mean '{color.primary}' or '{color.primary-hover}'?
      └─ one line, actionable, and computed from your actual token set
```

> **🟢 In plain words**
>
> Each code is a permanent name for one specific problem. The number never changes and is never
> reused, so you can search this page, search the codebase, or search your CI logs and get the same
> answer. Passages marked ${like this} below are placeholders — at runtime girih
> fills in your file name, token path, or component name.

All
Errors
Warnings

## The six families

Codes are partitioned by the package that raises them. The range tells you where to look in the
source before you have read a word of the message.

| Range | Owner | Covers | Codes |
| --- | --- | --- | --- |
| [`GIRIH1xxx`](#family-1) | `core + cli` | Workspace, config, manifest, ds.lock | 14 |
| [`GIRIH2xxx`](#family-2) | `tokens` | Parse, overlay, alias resolution, tier validation | 20 |
| [`GIRIH3xxx`](#family-3) | `generator-css` | CSS emission | 3 |
| [`GIRIH4xxx`](#family-4) | `spec` | Component contracts and extensions | 31 |
| [`GIRIH5xxx`](#family-5) | `generator-react` | React emission | 1 |
| [`GIRIH6xxx`](#family-6) | `cli` | Build and publish | 2 |

> **🟡 Watch out — Stay in your range, and never reuse a retired number**
>
> If you add a diagnostic, it belongs in the range of the package raising it. Reusing a retired
> number is worse than skipping one: somebody's CI filter, runbook, or test is still matching on the
> old meaning. Gaps in the numbering are free; collisions are not.

---

## The full reference

### `GIRIH1xxx` — Workspace and bookkeeping

Owned by `core + cli` · 14 codes

Something about the shape of your workspace, rather than the design system itself: the config
file is missing or malformed, a brand points at a file that is not there, the manifest or
`ds.lock` is corrupt, or a generated file was edited by hand. These are the codes you
hit on day one and then rarely again — with one exception,
`GIRIH1010`, which is the drift gate and will visit you regularly.

GIRIH1001
error
packages/girih-core/src/config.ts:92

No ds.config.ts found in ${root}

Run `girih init` to scaffold a workspace, or cd into an existing one.

GIRIH1002
error
packages/girih-core/src/config.ts:108

Failed to load ${configPath}: ${message.split('\n')[0]}

GIRIH1003
error
packages/girih-core/src/config.ts:185

Config is missing 'name' (the published package name, e.g. '@acme/design-system').

GIRIH1004
error
packages/girih-core/src/config.ts:160

Brand '${brand.name}' points at a missing token file: ${brand.tokensFile}

Create the overlay file (an empty {} is valid) or fix brands.definitions.${brand.name}.tokens.

GIRIH1005
error
packages/girih-core/src/config.ts:194

Config must define at least one brand under brands.definitions.

GIRIH1006
error
packages/girih-core/src/config.ts:201

brands.default ('${raw.brands?.default}') is not a key of brands.definitions.

Known brands: ${Object.keys(definitions).join(', ')}

GIRIH1007
error
packages/girih-core/src/config.ts:176

ds.config.ts must default-export a config object (use defineConfig).

GIRIH1008
error
packages/girih/src/cli.ts:352

Unknown brand '${options.brand}'.

Known brands: ${[...build.graphs.keys()].join(', ')}.

GIRIH1010
warning
packages/girih/src/cli.ts:410

Generated file ${path} was edited by hand since the last \`girih generate\`.

Undo the edit or eject the component (M5); the next generate will refuse to overwrite it.

GIRIH1011
warning
packages/girih/src/cli.ts:402

.ds/manifest.json is corrupt or from an incompatible girih version — drift detection is disabled.

Delete .ds/manifest.json and regenerate.

GIRIH1012
error
packages/girih/src/cli.ts:115

'${name}' is recorded as ejected in ds.lock, but ${path} is missing.

Restore the file, or remove the '${name}' entry from ds.lock to return to generation.

GIRIH1013
error
packages/girih/src/cli.ts:89

ds.lock is corrupt or from an incompatible girih version.

ds.lock is machine-managed and committed — restore it from git history.

GIRIH1014
warning
packages/girih/src/cli.ts:132

'${name}' was ejected from a different spec/template than the current one — the fork may not honor the contract anymore.

Review the fork against the current spec, or re-eject after removing the ds.lock entry (`girih forks` reports this; the 3-way merge is not built yet).

GIRIH1015
warning
packages/girih/src/cli.ts:103

ds.lock records '${name}' as ejected, but no spec with that name exists — the fork is not generated.

Restore components/${name.charAt(0).toLowerCase()}${name.slice(1)}.spec.ts, or remove the entry and components/ejected/${name}.tsx.

### `GIRIH2xxx` — Tokens

Owned by `tokens` · 20 codes

Your design language does not hold together. A reference points at nothing, two tokens
reference each other in a loop, a reference points upward through the tiers, a brand tried to add
a token instead of overriding one, or two brands ended up with different token sets. Most of the
time the message names the exact path, and `GIRIH2030` also guesses what you meant.

GIRIH2001
error
packages/girih-tokens/src/parse.ts:26

Token file must be a JSON object at the top level.

GIRIH2002
error
packages/girih-tokens/src/parse.ts:79

Token or group name '${key}' contains a character forbidden by DTCG ('.', '{', '}').

GIRIH2003
error
packages/girih-tokens/src/parse.ts:99

'${[...segments, key].join('.')}' is a bare ${typeof child}; tokens must be objects with a $value.

Write it as { "$value": ${JSON.stringify(child)} }.

GIRIH2004
warning
packages/girih-tokens/src/engine.ts:67

Cannot infer the tier of '${file}' from its name — assuming 'semantic'.

Name token files global*.tokens.json / semantic*.tokens.json, or put component tokens under tokens/components/.

GIRIH2005
error
packages/girih-tokens/src/engine.ts:39

Cannot read token file: ${(error as Error).message}

GIRIH2006
error
packages/girih-tokens/src/engine.ts:55

No token files matched ${config.tokens.source.join(', ')} under ${config.root}.

Create tokens/global.tokens.json to get started.

GIRIH2007
warning
packages/girih-tokens/src/parse.ts:89

Token or group name '${key}' contains characters that do not translate cleanly to CSS variable names.

Stick to lowercase letters, digits, "-" and "_" so generated names stay predictable.

GIRIH2008
warning
packages/girih-tokens/src/parse.ts:67

Unknown '$'-prefixed property '${key}' is ignored (the '$' prefix is reserved by DTCG).

Known properties: ${[...KNOWN_DOLLAR_PROPS].join(', ')}. Rename the key if it was meant to be a group.

GIRIH2009
error
packages/girih-tokens/src/parse.ts:42

'${path}' is a token (it has a $value) but also contains '${key}' — tokens cannot nest groups or other tokens.

Move '${key}' out to a sibling group, e.g. '${path}-${key}'.

GIRIH2010
error
packages/girih-tokens/src/merge.ts:27

Token '${token.path}' is defined in both ${existing.file} and ${token.file}.

Every token path must have exactly one definition; brand overlays are the only sanctioned override.

GIRIH2011
error
packages/girih-tokens/src/merge.ts:44 (+1 more)

Token '${token.path}' is nested under '${shadowedAncestor}', which is itself a token (defined in ${tokens.get(shadowedAncestor)!.file}).

A path must be either a token or a group, never both — rename one of them.

GIRIH2020
error
packages/girih-tokens/src/merge.ts:97

Brand overlay introduces '${token.path}', which does not exist in the base token set.

Overlays may only override existing tokens. Define the token in tokens/ first, then override its value here.

GIRIH2021
error
packages/girih-tokens/src/merge.ts:108

Brand overlay changes the $type of '${token.path}' from '${baseToken.type}' to '${token.type}'.

A brand may change a token value, never its type — types are part of the shared contract.

GIRIH2030
error
packages/girih-tokens/src/resolve.ts:45

'${token.path}' references '{${ref}}', which does not exist.

GIRIH2031
error
packages/girih-tokens/src/resolve.ts:79

Circular token reference: ${cycle.map((p) => `'${p}'`).join(' → ')}.

Break the cycle by pointing one of these tokens at a raw value.

GIRIH2032
error
packages/girih-tokens/src/resolve.ts:145

Failed to resolve '${token.path}': ${(error as Error).message}

GIRIH2040
error
packages/girih-tokens/src/validate.ts:19

${token.tier} token '${token.path}' references ${target.tier} token '{${ref}}' — references must flow component → semantic → global, never upward.

GIRIH2041
warning
packages/girih-tokens/src/validate.ts:27

component token '${token.path}' references global token '{${ref}}' directly, skipping the semantic tier.

Introduce a semantic alias for '{${ref}}' so brands can re-theme it without touching component tokens.

GIRIH2042
error
packages/girih-tokens/src/validate.ts:39

'${token.path}' ($type '${token.type}') aliases '{${ref}}' which has $type '${target.type}'.

GIRIH2050
error
packages/girih-tokens/src/validate.ts:69

Brand '${brand}' does not resolve the same token set as '${brands[0]}':

### `GIRIH3xxx` — CSS emission

Owned by `generator-css` · 3 codes

Only three codes, and all three are about the output being untrustworthy rather than
malformed: generation failed outright, a composite value could not be flattened into a CSS
string, or two different token paths collapse to the same CSS variable name. The last one would
be a silent last-one-wins if girih did not stop for it.

GIRIH3001
error
packages/girih-generator-css/src/generate.ts:125

CSS generation failed for brand '${brand}': ${(error as Error).message}

GIRIH3002
error
packages/girih-generator-css/src/generate.ts:220

'${variable}' (brand '${brand}') has a composite value no CSS transform could flatten.

Give the token a supported $type (shadow, typography, border, transition, cubicBezier, fontFamily) or split it into scalar tokens.

GIRIH3003
error
packages/girih-generator-css/src/generate.ts:176

Tokens ${paths.map((p) => `'${p}'`).join(' and ')} both map to the CSS variable '${varName}'.

Rename one of them — CSS variable names are case-insensitive here and join segments with "-".

### `GIRIH4xxx` — Component contracts

Owned by `spec` · 31 codes

The largest family, because a contract has the largest surface a user can get wrong: names,
variants and their defaults, states the template cannot express, prop collisions, token
references that do not resolve for every brand, and extensions reaching past
`overridableTokens`. If you are writing contracts, this is your range.

GIRIH4001
error
packages/girih-spec/src/validate.ts:142

Component name '${ir.name}' must be PascalCase (it becomes an exported React identifier).

GIRIH4002
error
packages/girih-spec/src/extensions.ts:162 (+1 more)

Extension '${extension.name}' references '{${path}}', which ${
missing.length === graphs.size ? 'no brand resolves' : `brand(s) ${missing.join(', ')} do not resolve`
}.

GIRIH4003
warning
packages/girih-spec/src/extensions.ts:179 (+1 more)

Extension '${extension.name}' references global token '{${path}}' — extensions should consume semantic or component tokens.

GIRIH4005
error
packages/girih-spec/src/validate.ts:150

Component '${ir.name}' is defined more than once (${seen.get(ir.name)} and ${file}).

GIRIH4006
error
packages/girih-spec/src/validate.ts:250

Variant axis '${axis.axis}' of '${ir.name}' has no values.

GIRIH4007
error
packages/girih-spec/src/validate.ts:271

Variant axis '${axis.axis}' of '${ir.name}' defaults to '${axis.default}', which is not one of [${axis.values.join(', ')}].

GIRIH4008
error
packages/girih-spec/src/validate.ts:284

'${ir.name}' declares state '${state}', which the '${ir.template}' template does not implement.

States of '${ir.template}': ${implementableStates.join(', ') || '(none)'}. States are template capability flags, not arbitrary behavior.

GIRIH4009
error
packages/girih-spec/src/validate.ts:321

'${ir.name}' styles variant axis '${block.axis}', which is not declared under variants.

GIRIH4010
error
packages/girih-spec/src/validate.ts:331

'${ir.name}' styles '${block.axis}.${block.value}', but '${block.value}' is not a declared value of that axis.

GIRIH4011
error
packages/girih-spec/src/validate.ts:308 (+1 more)

'${ir.name}' styles state '${state.state}' under tokens.states, but does not declare that state.

GIRIH4012
error
packages/girih-spec/src/extensions.ts:150 (+1 more)

Extension '${extension.name}' has a malformed token reference '${ref}' at ${property} — expected '{token.path}'.

GIRIH4013
error
packages/girih-spec/src/validate.ts:204 (+3 more)

'${ir.name}' ${what} '${name}' is not a valid camelCase identifier — it becomes a React prop.

GIRIH4014
error
packages/girih-spec/src/ir.ts:28

'${spec.name}' has a non-string declaration for '${property}' at ${where} — every styled value must be a '{token.path}' reference.

GIRIH4015
warning
packages/girih-spec/src/validate.ts:356

'${ir.name}' styles unknown CSS property '${property}' at ${where} — it will be emitted verbatim.

GIRIH4016
error
packages/girih-spec/src/validate.ts:296

'${ir.name}' maps aria attributes to state '${aria.state}', which it does not declare under states.

GIRIH4018
warning
packages/girih-spec/src/validate.ts:172

'${ir.name}' renders '', which is not a known host element.

Known elements: ${[...KNOWN_ELEMENTS].join(', ')}.

GIRIH4020
error
packages/girih-spec/src/load.ts:38

Failed to load spec: ${(error as Error).message}

GIRIH4021
error
packages/girih-spec/src/load.ts:48

${file} does not default-export a component spec.

Export the result of defineSpec({...}) from '@faravahar/girih'.

GIRIH4022
error
packages/girih-spec/src/load.ts:60

${file} contains a non-serializable value at '${impure}' — specs must be pure data.

Remove functions, dates, class instances, and symbols; a spec is a contract, not a program.

GIRIH4030
error
packages/girih-spec/src/extensions.ts:46

${file} does not default-export a variant extension.

Export the result of defineVariant({ name, extends, tokens }) from '@faravahar/girih'.

GIRIH4031
error
packages/girih-spec/src/extensions.ts:57

Failed to load extension: ${(error as Error).message}

GIRIH4032
error
packages/girih-spec/src/extensions.ts:85

Extension name '${extension.name}' must be PascalCase (it becomes an exported React identifier).

GIRIH4033
error
packages/girih-spec/src/extensions.ts:94

Extension '${extension.name}' collides with an existing component or extension.

GIRIH4034
error
packages/girih-spec/src/extensions.ts:106

Extension '${extension.name}' extends '${extension.extends}', which is not in the component catalog.

Known components: ${[...byName.keys()].join(', ') || '(none)'}.

GIRIH4035
error
packages/girih-spec/src/extensions.ts:116

'${extension.extends}' does not allow extensions (extensibility.allowExtends is false).

GIRIH4036
error
packages/girih-spec/src/extensions.ts:138

Extension '${extension.name}' overrides '${property}', which '${extension.extends}' does not list under extensibility.overridableTokens.

Overridable: ${base.extensibility.overridableTokens.join(', ') || '(none)'}.

GIRIH4037
error
packages/girih-spec/src/extensions.ts:125

'${extension.extends}' is a compound (dialog) component — extensions can only build on single-element components.

GIRIH4038
warning
packages/girih-spec/src/extensions.ts:187

Extension '${extension.name}' reaches into '{${path}}', another component's token namespace.

Use semantic tokens or '${componentNamespace(extension.extends)}.*' tokens so the extension survives refactors of unrelated components.

GIRIH4040
error
packages/girih-spec/src/validate.ts:161

'${ir.name}' uses template '${ir.template}', which this girih version does not ship.

Available templates: ${Object.keys(templates).join(', ')}.

GIRIH4041
error
packages/girih-spec/src/validate.ts:184

'${ir.name}' styles part '${part.part}', which the '${ir.template}' template does not have.

declaredParts.size > 0
? `Parts of '${ir.template}': ${[...declaredParts].join(', ')}.`
: `The '${ir.template}' template has no parts — use tokens.base.`

GIRIH4042
error
packages/girih-spec/src/validate.ts:236

'${ir.name}' declares variant axis '${axis.axis}', but the '${ir.template}' template only wires up: ${template.variantAxes.join(', ')}.

A silently-ignored axis would validate and then do nothing — declare only axes the template implements.

### `GIRIH5xxx` — React emission

Owned by `generator-react` · 1 code

One code. It fires when a contract maps ARIA attributes to a state that the chosen template
only expresses in CSS — so the attributes would not actually be wired up. A warning rather than an
error, because the component still works; it is just less accessible than the contract claims.

GIRIH5001
warning
packages/girih-generator-react/src/generate.ts:103

'${ir.name}' maps aria attributes to state '${aria.state}', which the '${ir.template}' template only expresses in CSS — the attributes are not wired.

### `GIRIH6xxx` — Build and publish

Owned by `cli` · 2 codes

One code, guarding the packaging step: there are no TypeScript sources to compile, which
means `girih generate react` has not run yet.

GIRIH6001
error
packages/girih/src/build.ts:60

No TypeScript sources in ${srcDir} — run \`girih generate react\` first.

GIRIH6002
error
packages/girih/src/build.ts:75

The generated package imports ${missing.map((m) => `'${m}'`).join(', ')}, which ${missing.length === 1 ? 'is' : 'are'} not installed.

Install the build prerequisites: npm install -D ${missing.join(' ')}

No diagnostics match that filter.

## An honest note on coverage

35 of the 71 diagnostics have no
`help` line. The project's own convention is that anything actionable should carry a
one-line suggestion, so that number is a real gap rather than a deliberate choice — most of the
missing ones are in the `GIRIH4xxx` contract family, where the message is often
self-explanatory but a pointer to the right field would still save time.

It is recorded here rather than smoothed over, because a reference that quietly implies full
coverage is worse than one that names its gaps. If you are adding a diagnostic, add the
`help`.

**[Terminal]**

```
# regenerate after changing any diagnostic
node docs/scripts/extract-diagnostics.mjs

# fail if the committed JSON is stale — suitable for a pre-commit hook or CI
node docs/scripts/extract-diagnostics.mjs --check
```

---

← [The code](06-the-code.md)  ·  [Contents](README.md)  ·  [Check yourself](08-quiz.md) →
