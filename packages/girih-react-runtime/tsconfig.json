{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "jsx": "react-jsx",
    "lib": ["ES2022", "DOM"],
    // Extends rather than replaces the base list — `types` does not merge.
    "types": ["node", "react"]
  },
  "include": ["src", "test"]
}
