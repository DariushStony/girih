# Fail at install time if Node is older than every package's declared floor, rather
# than somewhere confusing later. lint-staged sets the real floor (>=22.22.1).
# Understood by both npm and pnpm; pnpm-only settings live in pnpm-workspace.yaml.
engine-strict=true
