# Fail at install time if Node is older than every package's declared floor,
# rather than somewhere confusing later. lint-staged sets the real floor (>=22.22.1).
engine-strict=true

# Peers are declared deliberately here — react is a peer of the runtime and must be
# the consumer's choice, so silently installing one would hide a real mismatch.
auto-install-peers=false
